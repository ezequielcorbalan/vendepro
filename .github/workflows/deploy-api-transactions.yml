name: Deploy api-transactions

on:
  push:
    branches: [main]
    paths:
      - "reportes-backend/packages/api-transactions/**"
      - "reportes-backend/packages/core/**"
      - "reportes-backend/packages/infrastructure/**"
      - "reportes-backend/package.json"
      - "reportes-backend/package-lock.json"
      - ".github/workflows/deploy-api-transactions.yml"
      - ".github/workflows/_deploy-api.yml"
  workflow_dispatch:

concurrency:
  group: deploy-api-transactions
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-api.yml
    with:
      api_name: api-transactions
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
